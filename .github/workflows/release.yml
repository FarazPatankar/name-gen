concurrency:
  group: ${{ github.workflow }}

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
      - uses: ./.github/actions/prepare
      - run: pnpm build
      - run: npx should-semantic-release --verbose && npx release-it --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}

name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  id-token: write
